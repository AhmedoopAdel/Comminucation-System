@model List<WebApplication10.Models.User>

<div class="container my-5">
    <div class="row justify-content-center">
        <div class="col-lg-8">
            <h2 class="text-primary mb-4"><i class="bi bi-people-fill me-2"></i> قائمة الأصدقاء (@Model.Count)</h2>
            <hr />

            @if (Model.Any())
            {
                <div class="list-group">
                    @foreach (var user in Model)
                    {
                        <a href="/account/Profile/@user.Id" class="list-group-item list-group-item-action d-flex align-items-center shadow-sm mb-2 rounded-3 p-3">
                            <img src="@(user.ProfilePicUrl ?? "/images/default.png")"
                                 class="rounded-circle me-3 border border-secondary"
                                 style="width: 50px; height: 50px; object-fit: cover;"
                                 alt="@user.Username" />

                            <div class="flex-grow-1">
                                <h5 class="mb-0 fw-bold text-dark">@user.Username</h5>
                                <small class="text-muted">@(string.IsNullOrEmpty(user.Bio) ? "لا يوجد نبذة" : user.Bio)</small>
                            </div>

                            <i class="bi bi-chevron-left text-primary"></i>
                        </a>
                    }
                </div>
            }
            else
            {
                <div class="alert alert-info text-center" role="alert">
                    <i class="bi bi-info-circle-fill me-2"></i>
                    عذراً، لا يوجد لديك أصدقاء مشتركين حتى الآن. ابدأ بمتابعة أشخاص!
                </div>
            }
        </div>
    </div>
</div>